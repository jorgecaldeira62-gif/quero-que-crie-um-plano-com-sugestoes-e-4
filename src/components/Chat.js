const Chat = () => {
    const chatContainer = document.createElement('div');
    chatContainer.className = 'chat';
    chatContainer.innerHTML = 'Chat aqui...';

    return chatContainer;
};

export default Chat;
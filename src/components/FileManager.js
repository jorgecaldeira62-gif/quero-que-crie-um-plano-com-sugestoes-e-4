const FileManager = () => {
    const fileManagerContainer = document.createElement('div');
    fileManagerContainer.className = 'file-manager';
    fileManagerContainer.innerHTML = 'Gerenciador de arquivos aqui...';

    return fileManagerContainer;
};

export default FileManager;